{
  "name": "otp",
  "type": "registry:lib",
  "title": "Auth OTP (Prisma + Google)",
  "description": "OTP and Google OAuth authentication with Prisma ORM",
  "dependencies": [
    "@prisma/client",
    "resend",
    "@upstash/ratelimit",
    "@upstash/redis",
    "arctic",
    "zod"
  ],
  "devDependencies": ["prisma"],
  "registryDependencies": ["auth-otp-shared"]
}
